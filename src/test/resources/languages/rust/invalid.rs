fn broken() {
    let x = 5
    println!("{}", x);
}

struct MissingBrace {
    field: i32
// Missing closing brace

fn main() {
    if true {
        println!("test");
    // Missing closing brace for if
}