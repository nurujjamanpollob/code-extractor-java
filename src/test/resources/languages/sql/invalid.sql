SELECT * FROM users
WHERE id = 1
AND (username = 'admin' -- Missing closing paren

INSERT INTO users (id, name) VALUES (1, 'test' -- Missing closing paren

CREATE TABLE broken (
    id int
-- Missing closing paren for table definition